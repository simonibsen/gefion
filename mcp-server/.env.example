# Database connection
DATABASE_URL=postgresql://gefion:gefionpass@localhost:5432/gefion

# AlphaVantage API key (optional for ML, required for data ingestion)
ALPHAVANTAGE_API_KEY=your_api_key_here

# PostgreSQL port (for docker-compose)
POSTGRES_PORT=5432
