# Env and secrets
.env
.env.*

# Python artifacts
__pycache__/
.pytest_cache/
.venv/
*.pyc
*.egg-info/
dist/
build/

# Database files (root data dir only — unanchored `db/` used to swallow
# src/gefion/db/, leaving source files out of the repo)
/db/

# ML artifacts (generated output)
/datasets/
/models/

# OS noise
.DS_Store

# Claude Code — ignore user-specific state, track shared config and skills
.claude/*
!.claude/settings.json
!.claude/commands/
